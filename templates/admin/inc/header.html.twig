<nav class="navbar navbar-expand-lg navbar-dark bg-warning">
            <a class="navbar-brand" href="{{path('homePage')}}">Plateforme</a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="navbarColor01">

                <ul class="navbar-nav mr-auto">
                <li class="nav-item active">
                    <a class="nav-link" href="#"><img src="https://img.icons8.com/metro/26/000000/dashboard.png"/> Tableau de bord</a>
                </li>
                <li class="nav-item ">
                    <a class="nav-link" href="{{path('admin_ads_list')}}"><img src="https://img.icons8.com/ios-glyphs/30/000000/about.png"/> Annonces</a>
                </li>
                <li class="nav-item ">
                    <a class="nav-link" href="{{path('admin_bookings_list')}}"><img src="https://img.icons8.com/ios-glyphs/30/000000/hotel-chekc-out.png"/> Réservations</a>
                </li>
                <li class="nav-item ">
                    <a class="nav-link" href="{{path('admin_comments_list')}}"><img src="https://img.icons8.com/ios-glyphs/30/000000/speech-bubble-with-dots.png"/> Commentaires</a>
                </li>
                <li class="nav-item ">
                    <a class="nav-link" href="#"><img src="https://img.icons8.com/metro/26/000000/user-male-circle.png"/> Utilisateur</a>
                </li>
                </ul>


                <ul class="navbar-nav ml-auto align-items-center">
                {# si l'utilisateur est connecté app.user existe déjà dans symfony #}
                {% if app.user %}
                <li class="nav-item">
                    <a class="nav-link" href="{{path('homePage')}}" target='_blank'><img src="https://img.icons8.com/material-rounded/24/000000/show-property.png"/> Voir le site</a>
                </li>
                <li class="nav-item dropdown">
                    <a href="#" class="nav-link dropdown-toggle" id="accountDropdownLink" data-toggle="dropdown">
                        <img src="{{ app.user.avatar}}" alt="avatar de {{ app.user.fullName}}" class="avatar avatar-small"/>
                        {{ app.user.fullName}}
                    </a>
                    <div class="dropdown-menu dropdown-menu-right" aria-labelledby="accountDropdownLink">
                        <a class="dropdown-item" href="{{path('admin_account_logout')}}">Déconnexion</a>
                    </div>
                </li>
               {% else %}
                <li class="nav-item">
                    <a class="nav-link" href="{{path('account_register')}}">Inscription</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{path('account_login')}}">Connexion</a>
                </li>
                 {% endif %}
                </ul>
            
            </div>
        </nav>